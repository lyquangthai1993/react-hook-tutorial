import * as {{constantCase name}}_ACTION_TYPES from "./{{properCase name}}ActionTypes";
//default action
export const defaultAction = () => {
return {
type: {{constantCase name}}_ACTION_TYPES.{{constantCase name}}_DEFAULT
};
};
